package com.github.hanyaeger.tutorial.entities.powers;

import com.github.hanyaeger.api.Coordinate2D;
import com.github.hanyaeger.tutorial.entities.Bal;

public class VerbreedtBalk extends Power {

    protected VerbreedtBalk(String resource, Coordinate2D initialLocation) {
        super(resource, initialLocation);
    }

    void doePowerActie() {
//        Bal extraBal = new Bal()
//        Bal bal = new Bal(breakOutGame, spelerBalk, getWidth() / 2, (getHeight() / 4) * 3);
//        addEntity(bal);
    }
}